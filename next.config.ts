// next.config.js  
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com'], // Include any other domains you plan to use  
  },
};

module.exports = nextConfig;